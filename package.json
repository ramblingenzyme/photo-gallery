{
  "private": true,
  "packageManager": "pnpm@7.18.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.2",
    "lint-staged": "^12.0.4",
    "prettier": "^1.8.0"
  }
}