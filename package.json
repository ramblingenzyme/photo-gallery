{
  "name": "photo-gallery",
  "private": true,
  "packageManager": "yarn@3.3.0",
  "type": "module",
  "scripts": {
    "build:copy": "cp -r src/images src/css dist/",
    "format": "prettier --write .",
    "postinstall": "husky install"
  },
  "lint-staged": {
    "src/**/*.{js,less}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "ejs": "^3.1.8",
    "express": "^4.18.2",
    "husky": "^8.0.2",
    "image-size": "^1.0.2",
    "less": "^4.1.3",
    "lint-staged": "^13.0.4",
    "prettier": "^2.8.0"
  }
}
